# Repository Text for the Book Chapter

The URL below becomes active after the package is uploaded to the recommended
repository name:

<https://github.com/Mind-Twister-Wizard/HumanGate-Q>

## Recommended code-availability statement

> **Code availability.** The HumanGate-Q implementation, configuration,
> automated tests, trained model, per-workflow decision records, and the tables
> and figures supporting this chapter are available at
> https://github.com/Mind-Twister-Wizard/HumanGate-Q. The repository also
> provides a one-command reproduction workflow and records the dataset hash,
> random seed, data partitions, software environment, and selected operating
> thresholds. The Kaggle dataset is not redistributed and is retrieved from its
> original source.

## Short footnote version

> Code and reproducibility artifacts:
> https://github.com/Mind-Twister-Wizard/HumanGate-Q

## Suggested placement

Place the full statement in a **Code Availability** or **Data and Code
Availability** subsection immediately before the references, subject to the
editor's template. A footnote can be placed at the first mention of
HumanGate-Q if no availability section is permitted.

## Before final submission

1. Upload this package to the exact repository URL above.
2. Confirm that the public URL opens in a private/incognito browser window.
3. Create a GitHub release tagged `v2.1.0` so the cited implementation is fixed.
4. Replace the software citation with the final chapter citation after the
   publisher supplies the book title, editors, pages, year, and DOI.
5. For a preservation DOI, archive the GitHub release with a repository
   preservation service and add the DOI badge to `README.md`.
